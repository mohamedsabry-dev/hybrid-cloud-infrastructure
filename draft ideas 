So , 

Define the scope 

Phase 1 Should have 1 stack to deploy core things like ( s3 buket of tf , dynamo db , oicd rule , main terraform rule assume with admin inline policy )
Then , from terraform, i should create the cloud trail , the other assume roles with the limited policies and scope for specifc tasks no other one will have bug role 

but the admin assume role should be restricted on assumption only on specific branch ? or how to manage that ?



# Create new aws account for prod
# use current aws account for dev (wipe out everything only keep the config files)

# Follow clean seperation  for branches dev and main 
# Follow Dev first deploy (
        1. on aws account prod , will create 1 admin web user and secure the root and then use cloudformation to deploy terraform state buket , dynamo db for tf state locking , oicd rule , terraform admin assyme role with admin inline policy to prevent edit on these resources )
        2. use terraform to deploy rest of the infra include the limted assume roles , and only limt the use of admin assume role to specific branch ( main  branch) 
        3. on dev account will create one admin user and secure the root and use cloudformation to deploy the oicd and assume role with admin inline policy to prevent destrotuy the oicd ) 
        4. use terraform to deploy rest of the infra on dev account 

        will we use different tf state buket for dev and prod ? no , its better to keep one buket and use different folder 
        anyway we will need cross account access ( iam role assume or s3 backuet read allow , not put but get and read i think ) 
        because dev accounts will or may have readonly of the whole prod account to be able to read the tf state and do the plan and apply


        after finish establish the trust and cross acout and role access , this end of bootstrap phase. 

        then day 1 , we use tf dev to create multiple assume roles for seperate domains , like networking , security , compute , and secret management [dev token] 
        each role will have limited policy to do only specific tasks
        then we will create the same roles on prod but with same limited policies 
        each account manage their secrets independently
        then we will create the rest of the infra using tf
)
# Dont link the stack with any ci/cd , its only day 0 bootstrap phase. no need to edit on it later. "let oicd prod trust main branch only " and "oicd dev trust dev branch only " if you will enable ci/cd later "but dont enable it in gerneral " 

# then for proxmox , we have prod "the whole server " 
# dev proxmox , small nested 6GB server just to test images, vms, code, creation of conaitner , and then destroy , before push to prod proxmox
the scope of this project is to establish a robust and secure infrastructure deployment process using Terraform and AWS services, ensuring a clear separation between development and production environments. The focus will be on creating a scalable, maintainable, and secure setup that allows for efficient management of resources while adhering to best practices in infrastructure as code (IaC).

