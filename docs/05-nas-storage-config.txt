================================================================================
                    NAS STORAGE CONFIGURATION
================================================================================

Device: ASUSTOR FLASHSTOR 6 FS6706T
Capacity: 1.8TB usable (EXT4)
Management UI: https://10.0.5.120:8001

================================================================================
                         STORAGE NETWORK
================================================================================

Storage Plane (Dumb Switch only, NO routing)
─────────────────────────────────────────────────────────────────────────────────
VLAN 40 - Storage Data: 10.0.40.0/24
├── NO Gateway (isolated L2 network)
├── NAS storage interface:  10.0.40.120
├── Prod Proxmox storage:   10.0.40.100
└── Dev Proxmox storage:    10.0.40.110

NAS Interface Configuration
─────────────────────────────────────────────────────────────────────────────────
Server name: NAS-Storage
Default Gateway: LAN 2

Interface │ IP           │ Subnet        │ Gateway   │ Speed   │ Purpose
──────────┼──────────────┼───────────────┼───────────┼─────────┼────────────
LAN 2     │ 10.0.5.120   │ 255.255.255.0 │ 10.0.5.1  │ 100Mb   │ Management
LAN 1     │ 10.0.40.120  │ 255.255.255.0 │ --        │ 1000Mb  │ Storage

Connection Paths:
├── LAN 2 → AP → ER605 Port 3 → VLAN 5 (untagged)
└── LAN 1 → Dumb switch → Storage network (isolated, no routing)

================================================================================
                         NAS FIREWALL (ADM Defender)
================================================================================

Profile: Security_Restriction_for_Mac_Mini
─────────────────────────────────────────────────────────────────────────────────

LAN 2 (Management - 10.0.5.x):
├── Allow 192.168.0.223 (Mac Mini home network)
├── Allow 10.0.5.223 (Mac Mini mgmt VLAN)
└── Deny All

LAN 1 (Storage - 10.0.40.x):
├── Allow 10.0.40.100 (PROD Proxmox storage)
├── Allow 10.0.40.110 (DEV Proxmox storage)
└── Deny All

Security Model:
├── Servers access NAS storage (10.0.40.x) but NOT NAS management (10.0.5.120)
├── VMs/LXCs work because storage traffic goes through Proxmox host IP, not VM IP
└── Only Mac Mini can manage NAS via web UI

[TESTED 2026-02-13]
├── PROD (10.0.40.100) → NAS storage (10.0.40.120): PASS
├── DEV (10.0.40.110) → NAS storage (10.0.40.120): PASS
├── PROD (10.0.5.100) → NAS mgmt (10.0.5.120): BLOCKED
├── DEV (10.0.5.110) → NAS mgmt (10.0.5.120): BLOCKED
└── Mac Mini → NAS mgmt (10.0.5.120): PASS

================================================================================
                         NFS SERVICE CONFIGURATION
================================================================================

STEP 1: Enable NFS Service
─────────────────────────────────────────────────────────────────────────────────
1. Login to NAS web UI: https://10.0.5.120:8001
2. Go to: Services → NFS
3. Enable NFS
4. Apply

STEP 2: Create Shared Folders
─────────────────────────────────────────────────────────────────────────────────
Go to: Access Control → Shared Folders → Add

Folder Name  │ Volume        │ Recycle Bin │ Description
─────────────┼───────────────┼─────────────┼─────────────────────
shared-iso   │ Volume1(EXT4) │ Enable      │ ISOs and templates
prod-storage │ Volume1(EXT4) │ Enable      │ PROD VM storage
dev-storage  │ Volume1(EXT4) │ Enable      │ DEV VM storage

STEP 3: Configure NFS Permissions (IP-based isolation)
─────────────────────────────────────────────────────────────────────────────────
For each folder: Select folder → Edit → NFS Privileges tab → Add

shared-iso (both servers, read/write):
─────────────────────────────────────────────────────────────────────────────────
Client Address │ Privilege    │ root Mapping │ Async
───────────────┼──────────────┼──────────────┼──────
10.0.40.100    │ Read & Write │ root (0)     │ Yes
10.0.40.110    │ Read & Write │ root (0)     │ Yes

prod-storage (PROD only):
─────────────────────────────────────────────────────────────────────────────────
Client Address │ Privilege    │ root Mapping │ Async
───────────────┼──────────────┼──────────────┼──────
10.0.40.100    │ Read & Write │ root (0)     │ Yes

dev-storage (DEV only):
─────────────────────────────────────────────────────────────────────────────────
Client Address │ Privilege    │ root Mapping │ Async
───────────────┼──────────────┼──────────────┼──────
10.0.40.110    │ Read & Write │ root (0)     │ Yes

Mount Paths (auto-generated):
├── /volume1/shared-iso
├── /volume1/prod-storage
└── /volume1/dev-storage

ISOLATION:
├── PROD (10.0.40.100) cannot access dev-storage
├── DEV (10.0.40.110) cannot access prod-storage
└── NFS rejects connections from unauthorized IPs

Local User Access (Folders → Access Rights tab):
├── shared-iso: admin Read/Write (upload ISOs via NAS File Manager)
├── prod-storage: admin Read Only (browse only)
└── dev-storage: admin Read Only (browse only)

[TESTED 2026-02-13]

================================================================================
                         PROXMOX NFS STORAGE SETUP
================================================================================

STEP 4: Add Storage to Proxmox
─────────────────────────────────────────────────────────────────────────────────
On each Proxmox server: Datacenter → Storage → Add → NFS

On DEV server (10.0.5.110):
─────────────────────────────────────────────────────────────────────────────────
ID           │ Server      │ Export               │ Content
─────────────┼─────────────┼──────────────────────┼──────────────────────────────
nas-iso      │ 10.0.40.120 │ /volume1/shared-iso  │ ISO image, Container template
nas-dev-data │ 10.0.40.120 │ /volume1/dev-storage │ Disk image, Container, Backup

On PROD server (10.0.5.100):
─────────────────────────────────────────────────────────────────────────────────
ID            │ Server      │ Export                │ Content
──────────────┼─────────────┼───────────────────────┼──────────────────────────────
nas-iso       │ 10.0.40.120 │ /volume1/shared-iso   │ ISO image, Container template
nas-prod-data │ 10.0.40.120 │ /volume1/prod-storage │ Disk image, Container, Backup

STEP 5: Verify from Proxmox
─────────────────────────────────────────────────────────────────────────────────
```bash
# Check NFS exports are visible
showmount -e 10.0.40.120

# Check storage is mounted
df -h | grep nas
```

Proxmox auto-creates subdirectories:
├── images/         → VM disk files
├── dump/           → Backup files
├── private/        → Snippets
├── template/iso/   → ISO files
└── template/cache/ → Container templates

IMPORTANT: When uploading ISOs via NAS File Manager, place them in:
  `shared-iso/template/iso/` (NOT in root or template/)
  Proxmox only scans template/iso/ for ISO images.

[TESTED 2026-02-13] NAS storage working on DEV and PROD

================================================================================
                         STORAGE CAPACITY ALLOCATION
================================================================================

NAS Total: 1.8TB usable
─────────────────────────────────────────────────────────────────────────────────
Share        │ Estimated Size │ Purpose
─────────────┼────────────────┼─────────────────────────────────────────────────
shared-iso   │ ~50GB          │ ISOs, container templates (shared)
prod-storage │ ~600GB         │ PROD VM disks, backups, PVs
dev-storage  │ ~400GB         │ DEV VM disks, backups, PVs
Reserved     │ ~750GB         │ Future expansion, snapshots

================================================================================
                              STATUS: COMPLETE
================================================================================

[x] NAS interfaces configured (Management + Storage)
[x] NAS firewall configured (ADM Defender)
[x] NFS service enabled
[x] Shared folders created with IP-based isolation
[x] Proxmox NFS storage added (both servers)
[x] Storage isolation verified (PROD/DEV separation)
