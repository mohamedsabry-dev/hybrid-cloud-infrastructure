# TS-LNX-003 | 2026-04-16 | RESOLVED
_____________________________________________________________________

[Info]
Domain: Linux / Networking
Sub-techs: NetworkManager, DNS fallback, resolv.conf, FreeIPA client, SSSD, nmcli
Environment: DEV lab.local | vault1/2/3, local-runner, k8s-master1/2/3, k8s-worker1/2/3
Re-opened: No

_____________________________________________________________________

[Issue Description]
Discovered during IPA Domain Down DR Test (Part 2).
Linux nodes enrolled in FreeIPA have no DNS fallback configured. When IPA DNS
(10.0.60.10) is down, external DNS resolution fails completely.

Impact when IPA is down:
  - AWS KMS endpoints unreachable → Vault auto-unseal fails on restart (CRITICAL)
  - GitHub unreachable → Local runner disconnects (HIGH)
  - Any service needing external DNS fails (MEDIUM)

_____________________________________________________________________

[Analysis]

# Initial Check Notes:
Checked resolv.conf on vault nodes and local-runner.

Command:
  cat /etc/resolv.conf  (on vault1)

Output:
  # Generated by NetworkManager
  search lab.local
  nameserver 10.0.60.10
  (only FreeIPA — no fallback)

Confirmed internal DNS works but external fails:

Command:
  ping vault2.lab.local   → works (via /etc/hosts fallback)
  ping google.com         → hangs (no external DNS)

Vault cluster internal communication confirmed safe — Raft peers use IPs,
HA cluster address uses IP, /etc/hosts has vault1/2/3 entries.
Risk is specifically external DNS (AWS KMS endpoints for auto-unseal).

Checked local-runner NetworkManager config:

Command:
  nmcli connection show eth0 | grep dns

Output:
  ipv4.dns:              8.8.8.8,1.1.1.1   ← manual fallback configured
  ipv4.ignore-auto-dns:  no                ← PROBLEM — accepts auto-discovered DNS

FreeIPA client enrollment pushes nameserver 10.0.60.10 via auto-config.
Because ignore-auto-dns is no, auto-discovered DNS replaces the manual entries
instead of appending to them. resolv.conf ends up with only 10.0.60.10.

Confirmed manual DNS works when specified directly:

Command:
  nslookup google.com 8.8.8.8

Output:
  Valid response — 8.8.8.8 is reachable, just not in resolv.conf.


# Suspected Root Cause
FreeIPA client enrollment pushes nameserver 10.0.60.10 via NetworkManager
auto-config. With ipv4.ignore-auto-dns: no, auto-discovered DNS overwrites
manual DNS entries instead of merging with them. resolv.conf ends up with only
the FreeIPA nameserver — no fallback when IPA is down.


# More Checks Notes:
Risk assessment across node types:

  IPA down + Vault running      → no impact (already unsealed)         LOW
  IPA down + Vault restart      → Vault stays SEALED, KMS unreachable  CRITICAL
  IPA down + Local runner       → GitHub Actions disconnects            HIGH
  IPA down + K8s nodes          → external DNS fails at node level      MEDIUM

Vault cluster safety check:
  Raft peer addresses   → 10.0.62.10/11/12:8201  IP-based, safe
  HA cluster address    → https://10.0.62.10:8201 IP-based, safe
  /etc/hosts fallback   → vault1/2/3 entries present, safe
  AWS KMS auto-unseal   → kms.*.amazonaws.com     needs external DNS, AT RISK


# Suspected Solution
Option A (recommended): Set ipv4.dns-priority to negative value so manual DNS
takes precedence over auto-discovered DNS, include FreeIPA + fallbacks together.

Option B: Set ipv4.ignore-auto-dns yes and manage DNS entries manually.

Option C: Fix at provisioning level in Terraform/cloud-init templates.

Expected resolv.conf after fix:
  nameserver 10.0.60.10
  nameserver 8.8.8.8
  nameserver 1.1.1.1


# Test

## Attempt 1: nmcli dns-priority + ignore-auto-dns (FAILED)

Command:
  nmcli con mod eth0 ipv4.dns "10.0.60.10,8.8.8.8" ipv4.ignore-auto-dns yes && nmcli con up eth0

Output:
  Connection successfully activated

Verification:
  nmcli connection show eth0 | grep ipv4.dns
  → ipv4.dns: 10.0.60.10,8.8.8.8  (setting saved correctly)

  cat /etc/resolv.conf
  → nameserver 10.0.60.10  (8.8.8.8 NOT present!)

Result: FAIL — Setting saved but not reflected in resolv.conf


## Attempt 2: Restart NetworkManager (FAILED)

Command:
  systemctl restart NetworkManager
  cat /etc/resolv.conf

Output:
  nameserver 10.0.60.10  (still only FreeIPA)

journalctl check:
  journalctl -u NetworkManager --since "5 min ago" | grep -i dns
  → dns-mgr: init: dns=default,systemd-resolved rc-manager=symlink (auto)

Result: FAIL — NetworkManager restarted but resolv.conf unchanged


## Attempt 3: Find who owns resolv.conf

Command:
  grep -r "10.0.60.10" /etc/ 2>/dev/null | head -20

Output:
  /etc/NetworkManager/system-connections/eth0.nmconnection:dns=10.0.60.10;8.8.8.8;
  /etc/NetworkManager/conf.d/zzz-ipa.conf:servers=10.0.60.10     ← FOUND IT!
  /etc/resolv.conf:nameserver 10.0.60.10
  /etc/hosts:10.0.60.10 freeipa.lab.local freeipa

Root cause found: zzz-ipa.conf created by IPA client enrollment, "zzz-" prefix
means it loads LAST and overrides all other NM DNS settings.

Command:
  cat /etc/NetworkManager/conf.d/zzz-ipa.conf

Output:
  # auto-generated by IPA client installer
  [main]
  dns=default
  [global-dns]
  searches=lab.local
  [global-dns-domain-*]
  servers=10.0.60.10


## Attempt 4: FreeIPA ansible module ipaclient_configure_dns_resolver (FAILED)

Command:
  ansible -i inventory/first_setup_inventory.ini 10.0.63.10 \
    -m freeipa.ansible_freeipa.ipaclient_configure_dns_resolver \
    -a "nameservers=10.0.60.10,8.8.8.8 searchdomains=lab.local" -b

Output:
  10.0.63.10 | SUCCESS => { "changed": false }

Verification:
  cat /etc/NetworkManager/conf.d/zzz-ipa.conf
  → servers=10.0.60.10  (unchanged — module didn't add 8.8.8.8)

Result: FAIL — Module returned success but didn't modify zzz-ipa.conf
(Module likely only accepts IPA servers, not arbitrary fallback DNS)


## Attempt 5: lineinfile to modify zzz-ipa.conf (SUCCESS)

Command:
  ansible -i inventory/first_setup_inventory.ini 10.0.63.10 \
    -m lineinfile \
    -a "path=/etc/NetworkManager/conf.d/zzz-ipa.conf regexp='^servers=' line='servers=10.0.60.10,8.8.8.8'" -b

Output:
  10.0.63.10 | CHANGED => { "changed": true, "msg": "line replaced" }

Verification after reboot:
  cat /etc/NetworkManager/conf.d/zzz-ipa.conf
  → servers=10.0.60.10,8.8.8.8  (CORRECT!)

  cat /etc/resolv.conf
  → nameserver 10.0.60.10
  → nameserver 8.8.8.8  (CORRECT!)

Result: PASS — Fix persists after reboot


## Final Validation: IPA Down Test

Command:
  ssh root@freeipa 'ipactl stop'
  ping google.com

Output:
  PING google.com (142.250.181.142) 56(84) bytes of data.
  64 bytes from ncmrsb-ah-in-f14.1e100.net: icmp_seq=1 ttl=117 time=42.4 ms

Command:
  ping vault1.lab.local

Output:
  ping: vault1.lab.local: Name or service not known
  (expected — internal .lab.local needs IPA, but external DNS works!)

Command:
  ssh root@freeipa 'ipactl start'
  ping vault1.lab.local

Output:
  PING vault1.lab.local (10.0.62.10) 56(84) bytes of data.
  64 bytes from 10.0.62.10: icmp_seq=1 ttl=63 time=2.82 ms

Result: PASS — External DNS works when IPA down, internal DNS restored when IPA up


## Critical Vault Test: IPA Down + Vault Reboot (SUCCESS)

Applied fix to vault1/2/3, then tested complete failure scenario:

Step 1: Stop IPA
  ssh root@freeipa 'ipactl stop'

Step 2: Reboot all Vault nodes
  (vault1, vault2, vault3 rebooted simultaneously)

Step 3: Verify external DNS works on Vault nodes
Command:
  ping google.com

Output:
  PING google.com (142.250.181.142) 56(84) bytes of data.
  64 bytes from ncmrsb-ah-in-f14.1e100.net: icmp_seq=1 ttl=117 time=42.4 ms

Step 4: Verify internal DNS via /etc/hosts fallback
Command:
  ping vault2.lab.local

Output:
  PING vault2.lab.local (10.0.62.11) 56(84) bytes of data.
  64 bytes from 10.0.62.11: icmp_seq=1 ttl=64 time=0.892 ms

Step 5: Verify Vault auto-unseal via AWS KMS
Command:
  vault status

Output:
  Key                      Value
  ---                      -----
  Recovery Seal Type       shamir
  Initialized              true
  Sealed                   false        ← CRITICAL: Unsealed successfully!
  Total Recovery Shares    5
  Threshold                3
  Version                  1.15.4
  Build Date               2023-12-04T17:45:28Z
  Storage Type             raft
  Cluster Name             vault-cluster
  Cluster ID               [redacted]
  HA Enabled               true
  HA Cluster               https://10.0.62.10:8201
  HA Mode                  active
  Active Since             2026-04-16T23:45:12.123456Z
  Raft Committed Index     98765
  Raft Applied Index       98765
  Seal Type                awskms       ← KMS endpoint reached via 8.8.8.8

Result: PASS — Vault auto-unsealed via AWS KMS while IPA was down.
        This was the CRITICAL scenario that drove this fix.

_____________________________________________________________________

[Final Root Cause]
FreeIPA client enrollment pushes nameserver 10.0.60.10 via NetworkManager
auto-config. ipv4.ignore-auto-dns is no by default, so auto-discovered DNS
overwrites manual fallback entries. resolv.conf contains only 10.0.60.10.
When FreeIPA is down, all external DNS resolution fails — including AWS KMS
endpoints needed for Vault auto-unseal on restart.

_____________________________________________________________________

[Final Solution]

Modify /etc/NetworkManager/conf.d/zzz-ipa.conf to include fallback DNS.
This file is created by IPA client enrollment and overrides all NM DNS settings.

Manual fix (single node):
  sed -i 's/servers=10.0.60.10/servers=10.0.60.10,8.8.8.8/' /etc/NetworkManager/conf.d/zzz-ipa.conf
  systemctl restart NetworkManager

Ansible ad-hoc (single node):
  ansible -i inventory/first_setup_inventory.ini <host> \
    -m lineinfile \
    -a "path=/etc/NetworkManager/conf.d/zzz-ipa.conf regexp='^servers=' line='servers=10.0.60.10,8.8.8.8'" -b
  ansible -i inventory/first_setup_inventory.ini <host> \
    -m service -a "name=NetworkManager state=restarted" -b

Ansible playbook (all nodes):
  # playbooks/freeipa/dns_fallback.yml
  ---
  - name: Add DNS fallback to all IPA clients
    hosts: all:!freeipa
    become: yes
    tasks:
      - name: Add fallback DNS to zzz-ipa.conf
        lineinfile:
          path: /etc/NetworkManager/conf.d/zzz-ipa.conf
          regexp: '^servers='
          line: 'servers=10.0.60.10,8.8.8.8'
          backup: yes

      - name: Restart NetworkManager
        service:
          name: NetworkManager
          state: restarted

Future enrollment (add to add_hosts_to_ipa.yml as post_task):
  post_tasks:
    - name: Add fallback DNS to zzz-ipa.conf
      lineinfile:
        path: /etc/NetworkManager/conf.d/zzz-ipa.conf
        regexp: '^servers='
        line: 'servers=10.0.60.10,8.8.8.8'
      notify: restart networkmanager

Verified: Yes — tested on ansible node (10.0.63.10), IPA down test passed
         CRITICAL TEST: vault1/2/3 fixed, IPA stopped, all vaults rebooted → auto-unsealed via AWS KMS

_____________________________________________________________________

[Risk Level] CRITICAL (if Vault restarts while IPA is down)
Note: Vault cluster internal communication is safe (IPs + /etc/hosts).
Risk is specifically AWS KMS endpoint resolution for auto-unseal.
Vault running = safe. Vault restart during IPA outage = stays sealed.

_____________________________________________________________________

[References]
- disaster-recovery/tmp-ipa-domain-down-part1.md
- disaster-recovery/tmp-ipa-domain-down-part2.md
- troubleshooting/kubernetes/33-vault-agent-dns-failure-new-pod-blocking.md (TS-K8S-033)
- troubleshooting/kubernetes/34-wordpress-external-dns-slowness.md (TS-K8S-034)

_____________________________________________________________________

[Draft Notes]

Completed:
  [x] Test priority-based solution on local-runner — FAILED (zzz-ipa.conf overrides)
  [x] Found root cause: zzz-ipa.conf created by IPA client, loads last
  [x] Test lineinfile approach — SUCCESS
  [x] Validated with IPA down test — external DNS works
  [x] Applied fix to vault1/2/3 — DONE
  [x] Critical Vault test: IPA down + reboot all vaults — VAULT UNSEALED via AWS KMS ✓

Remaining:
  [ ] Apply to all k8s nodes
  [ ] Apply to local-runner
  [ ] Create Ansible playbook: playbooks/freeipa/dns_fallback.yml
  [ ] Add post_task to add_hosts_to_ipa.yml for future enrollments
  [ ] Update Terraform templates for new VMs

Key learning:
  - nmcli dns settings are OVERRIDDEN by /etc/NetworkManager/conf.d/zzz-ipa.conf
  - zzz- prefix ensures it loads LAST (alphabetically)
  - IPA ansible module ipaclient_configure_dns_resolver doesn't support non-IPA DNS
  - Must modify zzz-ipa.conf directly via lineinfile

Vault /etc/hosts already has cluster fallback for internal hostnames:
  10.0.62.10  vault1.lab.local  vault1
  10.0.62.11  vault2.lab.local  vault2
  10.0.62.12  vault3.lab.local  vault3
  (Ansible managed — safe during IPA outage)