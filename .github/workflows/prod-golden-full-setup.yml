# =============================================================================
# PROD Golden Templates Full Setup
# =============================================================================
# Creates golden VM and LXC templates for cloning
# Manual setup required after each job - see post-build instructions
# =============================================================================

name: "[PROD] Golden Templates - Full Setup"

# -----------------------------------------------------------------------------
# Triggers: Manual dispatch or push to prod branch
# -----------------------------------------------------------------------------
on:
  workflow_dispatch:
  push:
    branches:
      - prod
    paths:
      - 'terraform/prod/proxmox/lxc/golden-template/**'
      - 'terraform/prod/proxmox/vms/golden-image/**'
      - '.github/workflows/prod-golden-full-setup.yml'

# -----------------------------------------------------------------------------
# Permissions: OIDC for AWS authentication
# -----------------------------------------------------------------------------
permissions:
  id-token: write
  contents: read

# -----------------------------------------------------------------------------
# Environment Variables
# -----------------------------------------------------------------------------
env:
  ENVIRONMENT: prod
  TF_WORKING_DIR_LXC: terraform/prod/proxmox/lxc/golden-template
  TF_WORKING_DIR_VM: terraform/prod/proxmox/vms/golden-image
  ANSIBLE_HOST: 10.0.53.10
  ANSIBLE_USER: root

# =============================================================================
# Jobs
# =============================================================================
jobs:

  # ---------------------------------------------------------------------------
  # Job 1: Create Golden VM
  # Creates base VM for cloning - requires manual setup after deployment
  # Lock: PROD_INFRA_GOLDEN_VM_LOCK
  # ---------------------------------------------------------------------------
  create-vm:
    name: "1. Create Golden VM"
    runs-on: mac-mini
    if: ${{ vars.PROD_INFRA_GOLDEN_VM_LOCK != 'true' }}

    defaults:
      run:
        working-directory: ${{ env.TF_WORKING_DIR_VM }}

    steps:
      - name: Code Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID_PROD }}:role/GitHubActions-Infrastructure-${{ env.ENVIRONMENT }}
          aws-region: ${{ vars.AWS_REGION_PROD }}

      - name: Fetch Proxmox Secrets
        run: |
          API_SECRET=$(aws secretsmanager get-secret-value \
            --secret-id prod/proxmox/terraform-token \
            --query SecretString --output text)
          echo "::add-mask::${API_SECRET}"

          TOKEN_ID=$(echo "$API_SECRET" | jq -r '.token_id')
          TOKEN_SECRET=$(echo "$API_SECRET" | jq -r '.token_secret')

          echo "::add-mask::${TOKEN_ID}"
          echo "::add-mask::${TOKEN_SECRET}"
          echo "::add-mask::${TOKEN_ID}=${TOKEN_SECRET}"

          echo "TF_VAR_proxmox_api_token=${TOKEN_ID}=${TOKEN_SECRET}" >> $GITHUB_ENV

      - name: Terraform Init
        run: terraform init -upgrade

      - name: Terraform Plan
        timeout-minutes: 3
        run: terraform plan -out=tfplan

      # 3-minute pause for manual review before applying
      - name: Review Window
        run: |
          echo "3-minute review window started at $(date)"
          echo "Environment: ${{ env.ENVIRONMENT }}"
          echo "Plan output is available above for auditor review"
          sleep 180
          echo "Review window ended at $(date)"

      - name: Terraform Apply
        id: apply
        run: terraform apply -auto-approve tfplan

      # Manual setup instructions after VM creation
      - name: Post-Build Setup Instructions
        if: steps.apply.outcome == 'success'
        run: |
          echo "=============================================="
          echo "  VM CREATED - MANUAL SETUP REQUIRED"
          echo "=============================================="
          echo ""
          echo "Step 1: Copy setup script to Proxmox host (from Mac)"
          echo "  scp proxmox/scripts/golden-vm-setup.sh admin_prod@pve-prod.lab.local:/tmp/"
          echo ""
          echo "Step 2: Copy script from Proxmox to Ansible LXC"
          echo "  # On Proxmox host"
          echo "  pct push 2001 /tmp/golden-vm-setup.sh /tmp/golden-vm-setup.sh"
          echo ""
          echo "Step 3: Open VM console in Proxmox UI and pull script from Ansible"
          echo "  # Login as root in Proxmox web console"
          echo "  scp ${{ env.ANSIBLE_USER }}@${{ env.ANSIBLE_HOST }}:/tmp/golden-vm-setup.sh /tmp/"
          echo "  chmod +x /tmp/golden-vm-setup.sh"
          echo "  /tmp/golden-vm-setup.sh"
          echo ""
          echo "Step 4: Shutdown VM after setup"
          echo "  shutdown -h now"
          echo ""
          echo "Step 5: Clone and convert to template (on Proxmox host)"
          echo "  # Clone source VM to create template (keeps source VM for TF state)"
          echo "  qm clone 9000 9001 --name rocky10-golden-template --full"
          echo "  qm template 9001"
          echo ""
          echo "NOTE: VM 9000 stays as source (Terraform managed)"
          echo "      VM 9001 is the template (manually created clone)"
          echo "      Other VMs clone from template 9001"
          echo "=============================================="

  # ---------------------------------------------------------------------------
  # Job 2: Create Golden LXC
  # Creates base LXC container for cloning - requires manual setup after deployment
  # Lock: PROD_INFRA_GOLDEN_LXC_LOCK
  # ---------------------------------------------------------------------------
  create-lxc:
    name: "2. Create Golden LXC"
    runs-on: mac-mini
    needs: create-vm
    if: ${{ always() && needs.create-vm.result != 'failure' && needs.create-vm.result != 'cancelled' && vars.PROD_INFRA_GOLDEN_LXC_LOCK != 'true' }}

    defaults:
      run:
        working-directory: ${{ env.TF_WORKING_DIR_LXC }}

    steps:
      - name: Code Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID_PROD }}:role/GitHubActions-Infrastructure-${{ env.ENVIRONMENT }}
          aws-region: ${{ vars.AWS_REGION_PROD }}

      - name: Fetch Proxmox Secrets
        run: |
          API_SECRET=$(aws secretsmanager get-secret-value \
            --secret-id prod/proxmox/terraform-token \
            --query SecretString --output text)
          echo "::add-mask::${API_SECRET}"

          TOKEN_ID=$(echo "$API_SECRET" | jq -r '.token_id')
          TOKEN_SECRET=$(echo "$API_SECRET" | jq -r '.token_secret')

          echo "::add-mask::${TOKEN_ID}"
          echo "::add-mask::${TOKEN_SECRET}"
          echo "::add-mask::${TOKEN_ID}=${TOKEN_SECRET}"

          echo "TF_VAR_proxmox_api_token=${TOKEN_ID}=${TOKEN_SECRET}" >> $GITHUB_ENV

          LXC_PWD=$(aws secretsmanager get-secret-value \
            --secret-id prod/golden-image/lxc-root-password \
            --query SecretString --output text)
          echo "::add-mask::${LXC_PWD}"
          echo "TF_VAR_root_password=${LXC_PWD}" >> $GITHUB_ENV

      - name: Terraform Init
        run: terraform init -upgrade

      - name: Terraform Plan
        timeout-minutes: 3
        run: terraform plan -out=tfplan

      # 3-minute pause for manual review before applying
      - name: Review Window
        run: |
          echo "3-minute review window started at $(date)"
          echo "Environment: ${{ env.ENVIRONMENT }}"
          echo "Plan output is available above for auditor review"
          sleep 180
          echo "Review window ended at $(date)"

      - name: Terraform Apply
        id: apply
        run: terraform apply -auto-approve tfplan

      # Manual setup instructions after LXC creation
      - name: Post-Build Setup Instructions
        if: steps.apply.outcome == 'success'
        run: |
          echo "=============================================="
          echo " LXC CONTAINER CREATED - MANUAL SETUP REQUIRED"
          echo "=============================================="
          echo ""
          echo "Step 1: Copy setup script to Proxmox host (from Mac)"
          echo "  scp proxmox/scripts/golden-lxc-setup.sh admin_prod@pve-prod.lab.local:/tmp/"
          echo ""
          echo "Step 2: Push script into container (on Proxmox host)"
          echo "  pct push 9010 /tmp/golden-lxc-setup.sh /tmp/golden-lxc-setup.sh"
          echo ""
          echo "Step 3: Enter container and run setup"
          echo "  pct enter 9010"
          echo "  chmod +x /tmp/golden-lxc-setup.sh"
          echo "  /tmp/golden-lxc-setup.sh"
          echo ""
          echo "Step 4: After setup completes, exit and stop container"
          echo "  exit"
          echo "  pct stop 9010"
          echo ""
          echo "Step 5: Create backup using vzdump (to local storage)"
          echo "  vzdump 9010 --compress gzip --storage local --mode stop"
          echo ""
          echo "Step 6: Move backup to template directory and rename"
          echo "  mv /var/lib/vz/dump/vzdump-lxc-9010-*.tar.gz /mnt/pve/nas-iso/template/cache/rocky-9-lxc-golden.tar.gz"
          echo ""
          echo "Step 7: Verify template is available"
          echo "  pveam list nas-iso"
          echo ""
          echo "Note: The template will be at: nas-iso:vztmpl/rocky-9-lxc-golden.tar.gz"
          echo "      Source container 9010 stays for future updates (Terraform managed)"
          echo "=============================================="
