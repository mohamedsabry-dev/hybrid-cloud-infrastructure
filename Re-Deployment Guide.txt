================================================================================
                    HYBRID CLOUD INFRASTRUCTURE
                      RE-DEPLOYMENT GUIDE (DEV)
================================================================================

Full rebuild from scratch. Time: ~4-6 hours

================================================================================
                         PHASE 0: FOUNDATION
================================================================================

NOTE: We have 2 AWS accounts (DEV + PROD). Steps below show DEV.
      Repeat with "prod" tag for PROD account.

--------------------------------------------------------------------------------

STEP 0.1: AWS ACCOUNT SETUP (per account)
|
+-- ROOT ACCOUNT:
|   +-- Enable MFA on root user
|   +-- Do NOT use root for daily operations
|
+-- CREATE ADMIN USER:
|   +-- IAM -> Users -> Create user
|   +-- Name: admin_dev (or admin_prod)
|   +-- Enable console access
|   +-- Attach: AdministratorAccess policy
|   +-- Enable MFA on admin user
|
+-- BOOTSTRAP STACK:
|   +-- Login as admin_dev
|   +-- CloudFormation -> Create Stack
|   +-- Upload: aws/deployment-stacks/bootstrap-dev.yaml
|   +-- Stack name: dev-bootstrap
|   +-- Check "I acknowledge that AWS CloudFormation might create IAM resources"
|
+-- CREATES:
    +-- OIDC Provider (GitHub federation)
    +-- S3 Bucket (hybrid-cloud-infrastructure-tf-state-dev)
    +-- DynamoDB Table (hybrid-cloud-infrastructure-tf-state-lock-dev)
    +-- TerraformPermissionsBoundary policy
    +-- GitHubActions-TerraformAdmin-dev role (security branch only)

--------------------------------------------------------------------------------

STEP 0.2: GITHUB SETUP
|
+-- VARIABLES (Settings -> Actions -> Variables):
|   |
|   +-- Account IDs:
|   |   +-- AWS_ACCOUNT_ID_DEV   = <dev-account-id>
|   |   +-- AWS_ACCOUNT_ID_PROD  = <prod-account-id>
|   |   +-- AWS_REGION           = eu-west-2
|   |
|   +-- Runner Config:
|   |   +-- DEV_GH_RUNNER_NAME   = dev-local-runner
|   |   +-- DEV_GH_RUNNER_LABELS = dev-local-runner
|   |   +-- PROD_GH_RUNNER_NAME  = prod-local-runner
|   |   +-- PROD_GH_RUNNER_LABELS= prod-local-runner
|   |
|   +-- Lock Variables (all = true, repeat for PROD_*):
|       +-- DEV_INFRA_GOLDEN_VM_LOCK, DEV_INFRA_GOLDEN_LXC_LOCK
|       +-- DEV_INFRA_ANSIBLE_LOCK, DEV_INFRA_LOCAL_RUNNER_LOCK
|       +-- DEV_INFRA_FREEIPA_LOCK, DEV_INFRA_K8S_MASTERS_LOCK
|       +-- DEV_INFRA_K8S_WORKERS_LOCK, DEV_INFRA_VAULT_LOCK
|       +-- DEV_SVC_DEPLOY_KEY_LOCK, DEV_SVC_ANSIBLE_SETUP_LOCK
|       +-- DEV_SVC_GH_RUNNER_LOCK, DEV_SVC_FREEIPA_SETUP
|
+-- SECRETS (Settings -> Actions -> Secrets):
|   +-- GH_ADMIN_PAT        (fine-grained PAT with admin:repo)
|   +-- DEV_GH_RUNNER_TOKEN (fresh token, expires ~1h)
|   +-- PROD_GH_RUNNER_TOKEN
|
+-- MAC-MINI RUNNER (Settings -> Actions -> Runners -> Add):
|   +-- mkdir -p ~/WorkSpace/actions-runner && cd ~/WorkSpace/actions-runner
|   +-- Download: actions-runner-osx-arm64-2.331.0.tar.gz
|   +-- ./config.sh --url <repo-url> --token <TOKEN> \
|       --labels self-hosted,macOS,ARM64,mac-mini --name Mohameds-Mac-mini
|   +-- ./svc.sh install && ./svc.sh start
|
+-- IAM ROLE -> BRANCH MAPPING:
    +------------------------------------------+------------------+-------------+
    | Role                                     | Trusted Branch   | Purpose     |
    +------------------------------------------+------------------+-------------+
    | GitHubActions-TerraformAdmin-dev         | dev-security     | IAM changes |
    | GitHubActions-TerraformAdmin-prod        | prod-security    | IAM changes |
    | GitHubActions-Infrastructure-dev         | dev              | Infra deploy|
    | GitHubActions-Infrastructure-prod        | prod             | Infra deploy|
    +------------------------------------------+------------------+-------------+

    Admin roles: Created by CloudFormation bootstrap (Step 0.1)
    Infra roles: Created by Terraform (Step 0.7 -> terraform/dev/aws/iam/)

--------------------------------------------------------------------------------

STEP 0.3: PROXMOX INSTALL (v9.1)
|
+-- Boot from: proxmox-ve_9.1-1.iso
|
+-- During install -> Name interfaces:
|   +-- Main Ethernet -> svc0   (trunk port, all VLANs)
|   +-- USB-Ethernet  -> stor0  (storage network)
|   +-- WiFi -> note name (e.g., wlp1s0)
|
+-- Set temp IP for initial access
|
+-- Complete install, reboot

--------------------------------------------------------------------------------

STEP 0.4: PROXMOX BOOTSTRAP
|
+-- SCRIPTS LOCATION: proxmox/scripts/
|   +-- bootstrap-dev.sh      (post-install bootstrap)
|   +-- bootstrap-prod.sh
|   +-- network-setup-dev.sh  (VLAN configuration)
|   +-- network-setup-prod.sh
|   +-- golden-vm-setup.sh    (run inside VM)
|   +-- golden-lxc-setup.sh   (run inside CT)
|
+-- RUN:
|   +-- scp proxmox/scripts/bootstrap-dev.sh root@<ip>:/tmp/
|   +-- ssh root@<ip>
|   +-- chmod +x /tmp/bootstrap-dev.sh && /tmp/bootstrap-dev.sh
|
+-- SAVE: API token output -> will store in AWS

--------------------------------------------------------------------------------

STEP 0.5: PROXMOX NETWORK
|
+-- scp proxmox/scripts/network-setup-dev.sh root@<ip>:/tmp/
+-- WIFI_PASSWORD="xxx" /tmp/network-setup-dev.sh
|
+-- CONFIGURES:
|   +-- WiFi -> 10.0.5.110 (management)
|   +-- svc0 -> VLAN bridge 60-65
|   +-- stor0.40 -> 10.0.40.110 (storage)
|
+-- REBOOT -> Access https://pve-dev.lab.local:8006

--------------------------------------------------------------------------------

STEP 0.6: NAS MOUNTS
|
+-- NAS: ASUSTOR FLASHSTOR 6 FS6706T
|   +-- Management: https://10.0.5.120:8001 (VLAN 5)
|   +-- Storage:    10.0.40.120 (VLAN 40)
|
+-- NFS SHARES:
|   +-- /volume1/shared-iso    -> ISOs, CT templates (both servers)
|   +-- /volume1/dev-storage   -> DEV VMs, backups (10.0.40.110 only)
|   +-- /volume1/prod-storage  -> PROD VMs, backups (10.0.40.100 only)
|
+-- Proxmox GUI -> Datacenter -> Storage -> Add -> NFS:
|
+-- nas-iso:      10.0.40.120:/volume1/shared-iso     (ISO, CT template)
+-- nas-dev-data: 10.0.40.120:/volume1/dev-storage    (Disk, CT, Backup)
|
+-- Verify: showmount -e 10.0.40.120
+-- Note: Upload ISOs to shared-iso/template/iso/ (not root)

--------------------------------------------------------------------------------

STEP 0.7: AWS SECRETS + IAM
|
|   CODE LOCATIONS:
|   +-- terraform/dev/aws/secrets/  -> Secret placeholders
|   +-- terraform/dev/aws/iam/      -> IAM roles + policies
|   +-- .github/workflows/dev-aws-*.yml -> Deployment workflows
|
+-- A) CREATE SECRET PLACEHOLDERS:
|   |
|   +-- TRIGGER: dev-aws-secrets.yml
|   |   +-- Push to dev branch (unlock DEV_INFRA_AWS_SECRETS_LOCK first)
|   |
|   +-- CREATES empty secrets (from terraform/dev/aws/secrets/main.tf):
|       +-- dev/proxmox/terraform-token
|       +-- dev/proxmox/ssh-admin-dev-password
|       +-- dev/golden-image/vm-root-password
|       +-- dev/golden-image/lxc-root-password
|       +-- dev/vm/break-glass-password
|       +-- dev/freeipa/admin-password
|       +-- dev/freeipa/dm-password
|       +-- dev/ansible/ssh-public-key
|       +-- dev/local-runner/ssh-public-key
|       +-- dev/super_bot/keytab
|
+-- B) POPULATE SECRETS (manual):
|   |
|   +-- Proxmox token (from Step 0.4 output):
|   |   aws secretsmanager put-secret-value --secret-id dev/proxmox/terraform-token \
|   |     --secret-string '{"token_id":"tf_dev@pve!terraform","token_secret":"xxx"}'
|   |
|   +-- Other secrets via Console or CLI:
|       +-- dev/proxmox/ssh-admin-dev-password -> admin_dev SSH password
|       +-- dev/golden-image/vm-root-password  -> strong password
|       +-- dev/golden-image/lxc-root-password -> strong password
|       +-- dev/vm/break-glass-password        -> emergency recovery
|       +-- dev/freeipa/admin-password         -> IPA admin password
|       +-- dev/freeipa/dm-password            -> IPA Directory Manager
|
+-- C) CREATE IAM ROLES:
    |
    +-- TRIGGER: dev-aws-iam.yml
    |   +-- Push to dev-security branch (uses TerraformAdmin role)
    |
    +-- CREATES (from terraform/dev/aws/iam/):
        +-- GitHubActions-Infrastructure-dev role
        +-- TerraformState-Dev policy (S3 + DynamoDB)
        +-- SecurityBoundary-Dev policy (blocks IAM mutations)

================================================================================
                    PHASE 1: INFRASTRUCTURE
================================================================================

ORDER: Golden -> Ansible -> Runner -> LXCs -> FreeIPA -> K8s

--------------------------------------------------------------------------------

STEP 1.1: GOLDEN IMAGES
|
+-- UNLOCK: DEV_INFRA_GOLDEN_VM_LOCK = false
|           DEV_INFRA_GOLDEN_LXC_LOCK = false
|
+-- TRIGGER: dev-golden-full-setup.yml
|
+-- WORKFLOW RUNS:
|   +-- Creates VM from ISO, runs golden-vm-setup.sh
|   +-- Creates LXC from CT template, runs golden-lxc-setup.sh
|   +-- Converts to templates (qm template 9000, pct template 9001)
|
+-- RESULT: Template 9000 (VM), 9001 (LXC)
+-- NOTE: Golden template workflows AUTO-LOCK after success

--------------------------------------------------------------------------------

STEP 1.2: ANSIBLE LXC
|
+-- UNLOCK: DEV_INFRA_ANSIBLE_LOCK = false
|           DEV_SVC_DEPLOY_KEY_LOCK = false
|           DEV_SVC_ANSIBLE_SETUP_LOCK = false
|
+-- TRIGGER: dev-ansible-full-setup.yml
|
+-- WORKFLOW:
|   +-- Clone LXC from template 9001
|   +-- Generate SSH key pair
|   +-- Store public key in AWS (dev/ansible/ssh-public-key)
|   +-- Add deploy key to GitHub repo (write access)
|   +-- Clone repo to /srv/repo
|
+-- RESULT: Ansible at 10.0.63.10, repo at /srv/repo

--------------------------------------------------------------------------------

STEP 1.3: LOCAL RUNNER
|
+-- GET fresh token: GitHub -> Settings -> Actions -> Runners -> New
+-- ADD SECRET: DEV_GH_RUNNER_TOKEN (expires in ~1 hour!)
|
+-- UNLOCK: DEV_INFRA_LOCAL_RUNNER_LOCK = false
|           DEV_SVC_GH_RUNNER_LOCK = false
|           DEV_SVC_RUNNER_TOOLS_LOCK = false
|
+-- TRIGGER: dev-local-runner-full-setup.yml
|
+-- WORKFLOW:
|   +-- Clone LXC from template 9001
|   +-- Create runner user, install runner
|   +-- Configure with: --name dev-local-runner --labels dev-local-runner
|   +-- Install and start service
|
+-- RESULT: dev-local-runner online in GitHub

--------------------------------------------------------------------------------

STEP 1.4: REMAINING LXCs
|
+-- UNLOCK + TRIGGER: dev-vault-full-setup.yml
|   +-- vault1-3 at 10.0.62.10-12
|
+-- UNLOCK + TRIGGER: dev-nginx-full-setup.yml
    +-- ex-nginx at 10.0.65.10

--------------------------------------------------------------------------------

STEP 1.5: FREEIPA
|
+-- UNLOCK: DEV_INFRA_FREEIPA_LOCK = false
|           DEV_SVC_FREEIPA_SETUP = false
|
+-- TRIGGER: dev-freeipa-full-setup.yml
|
+-- WORKFLOW:
|   +-- Creates VM from template 9000
|   +-- Runs FreeIPA server installation
|   +-- Configures DNS forwarders (8.8.8.8, 1.1.1.1)
|   +-- Sets UID range 60001-65500 (LXC compatible)
|
+-- RESULT: FreeIPA at 10.0.60.10, domain lab.local

--------------------------------------------------------------------------------

STEP 1.6: KUBERNETES
|
+-- UNLOCK: DEV_INFRA_K8S_MASTERS_LOCK = false
|           DEV_INFRA_K8S_WORKERS_LOCK = false
|
+-- TRIGGER: dev-k8s-full-setup.yml
|
+-- RESULT: Masters 10.0.61.10-12, Workers 10.0.64.10-12

================================================================================
                    PHASE 2: CONFIGURATION
================================================================================

STEP 2.1: ANSIBLE ADMIN SETUP
|
+-- ssh root@ansible.lab.local
|
+-- echo 'export ANSIBLE_CONFIG=/srv/repo/ansible/dev/ansible.cfg' >> ~/.bashrc
+-- source ~/.bashrc
|
+-- echo 'VAULT_PASSWORD' > ~/.ansible_vault
+-- chmod 600 ~/.ansible_vault
|
+-- git config --global pull.rebase true
+-- cd /srv/repo && git pull origin dev

--------------------------------------------------------------------------------

STEP 2.2: CREATE SUPER_BOT KEYTAB
|
+-- ssh root@freeipa.lab.local
+-- ipa-getkeytab -s freeipa.lab.local -p super_bot -k /tmp/sb.keytab
+-- base64 -w 0 /tmp/sb.keytab    # copy output
+-- rm /tmp/sb.keytab
|
+-- aws secretsmanager put-secret-value --secret-id dev/super_bot/keytab \
      --secret-string "PASTE_BASE64"

--------------------------------------------------------------------------------

STEP 2.3: RUN PLAYBOOKS
|
+-- kinit super_bot
|
+-- cd /srv/repo/ansible/dev
|
+-- ansible-playbook -i inventory/inventory.ini playbooks/common/baseos_fix.yml
+-- ansible-playbook -i inventory/inventory.ini playbooks/common/initial_packages.yml
+-- ansible-playbook -i inventory/inventory.ini playbooks/common/enable_ssh_password_auth.yml
|
+-- ansible-playbook -i inventory/inventory.ini playbooks/freeipa/add_hosts_to_ipa.yml
+-- ansible-playbook -i inventory/inventory.ini playbooks/freeipa/domain_config.yml
|
+-- ansible-playbook -i inventory/inventory.ini playbooks/freeipa/fix_lxc_krb5_keyring.yml

================================================================================
                         IP MAP
================================================================================

VLAN 5:   10.0.5.100   pve-prod (Proxmox)
          10.0.5.110   pve-dev (Proxmox)
          10.0.5.120   NAS (management)
VLAN 40:  10.0.40.100  pve-prod storage
          10.0.40.110  pve-dev storage
          10.0.40.120  NAS (storage)
VLAN 60:  10.0.60.10   freeipa
VLAN 61:  10.0.61.10-12 k8s-master1-3
VLAN 62:  10.0.62.10-12 vault1-3
VLAN 63:  10.0.63.10 ansible | 10.0.63.20 local-runner
VLAN 64:  10.0.64.10-12 k8s-worker1-3
VLAN 65:  10.0.65.10   ex-nginx

================================================================================
                         RUNNERS REFERENCE
================================================================================

+-------------+------------+-------------------+-----------------+
| Environment | IP Address | Runner Name       | Label           |
+-------------+------------+-------------------+-----------------+
| Mac Mini    | local      | Mohameds-Mac-mini | mac-mini        |
| Dev         | 10.0.63.20 | dev-local-runner  | dev-local-runner|
| Prod        | 10.0.53.20 | prod-local-runner | prod-local-runner|
+-------------+------------+-------------------+-----------------+

Workflow usage:
  runs-on: mac-mini          # For gh CLI, initial infra
  runs-on: dev-local-runner  # For dev service playbooks
  runs-on: prod-local-runner # For prod service playbooks

================================================================================
                         VERIFY
================================================================================

[ ] https://pve-dev.lab.local:8006 accessible
[ ] Templates 9000, 9001 exist
[ ] dev-local-runner online in GitHub
[ ] https://freeipa.lab.local accessible
[ ] kinit super_bot works
[ ] ansible managed_hosts -m ping all green

================================================================================
                      TROUBLESHOOTING -> /troubleshooting/
================================================================================

TS-001  LXC Kerberos keyring        TS-005  LXC UID mapping
TS-002  LXC NTP disabled            TS-006  Kerberos needs hostnames
TS-003  FreeIPA DNS                 TS-007  FreeIPA config gotchas
TS-004  VM SSH cloud-init

================================================================================
                      WORKFLOW LOCK PATTERN
================================================================================

SAFE TRIGGER MECHANISM (workaround for workflow_dispatch limitation):

Lock Variables:
  Pattern: {ENV}_INFRA_{NAME}_LOCK
  Example: DEV_INFRA_FREEIPA_LOCK

Values:
  true  -> Workflow skips execution (safe default)
  false -> Workflow runs on push

To trigger a workflow:
  1. Set variable to "false" in GitHub UI
  2. Push to branch (workflow runs)
  3. Set variable back to "true" (re-lock)

Auto-Lock (golden templates only):
  Golden VM/LXC workflows auto-lock after success.
  Other workflows keep manual control.

================================================================================
                      SECRET FLOW
================================================================================

GitHub Secrets (stored in GitHub):
  +-- GH_ADMIN_PAT        (PAT for deploy keys)
  +-- DEV_GH_RUNNER_TOKEN (runner registration)

AWS Secrets (fetched at runtime via OIDC):
  +-- dev/proxmox/terraform-token
  +-- dev/proxmox/ssh-admin-dev-password
  +-- dev/freeipa/admin-password
  +-- dev/super_bot/keytab

Flow:
  OIDC -> AWS -> Secrets Manager -> Mask -> TF_VAR -> Terraform -> Provider

================================================================================
                      CODE ARCHITECTURE: COPY-PROMOTE PATTERN
================================================================================

PATTERN CHOSEN: Folder Isolation (Copy-Promote)

  terraform/dev/aws/secrets/     <- DEV code (develop here)
  terraform/prod/aws/secrets/    <- PROD code (copy when ready)

  ansible/dev/playbooks/         <- DEV playbooks
  ansible/prod/playbooks/        <- PROD playbooks

  .github/workflows/dev-*.yml    <- DEV workflows
  .github/workflows/prod-*.yml   <- PROD workflows

--------------------------------------------------------------------------------

WHY NOT SINGLE-SOURCE (shared code + env variables)?

  Problem: If playbooks/terraform are shared, and you're halfway through
  developing a new feature in dev branch, then need to merge OTHER completed
  work to prod - the incomplete work comes along too.

  Example:
    ansible/playbooks/new_feature.yml  <- half-done in dev branch
    # You merge to prod for a different fix
    # Git brings the half-done playbook too

  With folder isolation:
    ansible/dev/playbooks/new_feature.yml   <- half-done, stays in dev
    ansible/prod/playbooks/                  <- clean, only complete work
    # Merge to prod branch - prod folder unaffected
    # Copy new_feature.yml to prod WHEN READY

--------------------------------------------------------------------------------

WHEN TO USE EACH:

  Copy-Promote (our choice):
    + Features take multiple commits
    + Work on many things in parallel
    + Solo developer = you are the gatekeeper
    + Explicit control over what goes to prod

  Single-Source (alternative):
    + Features are atomic (one commit = complete)
    + Strict PR review process with team
    + Feature flags to hide incomplete work

--------------------------------------------------------------------------------

DEV -> PROD PROMOTION WORKFLOW:

  1. Complete and test feature in dev folder
  2. Copy to prod folder, update env-specific values (IPs, tags)
  3. Use diff to verify only expected changes:

     # Terraform
     diff -r terraform/dev/aws/secrets terraform/prod/aws/secrets

     # Ansible
     diff -r ansible/dev/playbooks ansible/prod/playbooks

     # Workflows
     diff .github/workflows/dev-aws-secrets.yml .github/workflows/prod-aws-secrets.yml

  4. Commit and push to prod branch

--------------------------------------------------------------------------------

WHAT SHOULD DIFFER (expected):

  +-- Environment tags: "dev" vs "prod"
  +-- IP ranges: 10.0.6x.x vs 10.0.5x.x
  +-- Secret paths: dev/* vs prod/*
  +-- State bucket/table names: *-dev vs *-prod
  +-- Role names: *-dev vs *-prod
  +-- Branch triggers: dev vs prod

WHAT SHOULD NOT DIFFER (drift = bug):

  +-- Logic/structure of terraform resources
  +-- Playbook tasks and handlers
  +-- Workflow steps (except env-specific values)

================================================================================
