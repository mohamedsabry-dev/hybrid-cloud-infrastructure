# Dev - Proxmox Resources

Manages Proxmox VMs and infrastructure for the dev environment.

## Authentication

- Credentials fetched from AWS Secrets Manager (`dev/proxmox/terraform-token`)
- Uses `tf_dev@pve` user with admin on dev node only

## Current Resources

Placeholder - resources will be added as needed.

## Workflow

Deployed by: `.github/workflows/dev-proxmox-resources.yml`
