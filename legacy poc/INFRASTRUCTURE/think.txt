# re-organize the env , re build 
# Build Golden image on Master Cluster 
  # will be used for IPA Deploy - TrueNAS Deploy - and other internal env later 
  # Setup claude to do tasks for linear 
  # Learn TrueNAS 
  # Learn Vlan , will u divide on workstation level - esxi vsiwthch ? or pfsense ? or all or some . 
  # but i need ALl to access each other so need understand the point of VLANs 
  # Use the TF to push bootstrap guide and script with the golden images , and use tf as ansible for continues update on the config when needed , i may set ansible on my local runner mac mini , so from github it can also 
# tf can be used but should not be used , better to have ansible , so setup ansible on local runner ? use pssword or vault or what , more more integration .

