apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespaces
  - priority-classes
  - storage
  - vault
  - ingress
  - coredns
  # NOTE: remediation moved to apps/ - requires vault-agent injection
  # and must deploy AFTER vault-agent-injector is ready