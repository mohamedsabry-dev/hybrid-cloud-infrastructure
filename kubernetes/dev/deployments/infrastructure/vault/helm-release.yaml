apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata: 
  name: vault
  namespace: vault
spec:
  interval: 5m
  chart:
    spec:
      chart: vault
      sourceRef:
        kind: HelmRepository
        name: hashicorp
        namespace: vault
  values:
    global:
      externalVaultAddr: "https://vault.lab.local:8200"
    server:
      enabled: false
    injector:
      enabled: true

  # Proper fix later: Mount FreeIPA CA in helm-release.yaml:                                                        
  # injector:
  #   extraEnvironmentVars:
  #     VAULT_CACERT: /etc/ipa/ca.crt                                                                               
  #   volumes:
  #     - name: ipa-ca                                                                                              
  #       hostPath:                                                                                               
  #         path: /etc/ipa/ca.crt                                                                                 
  #   volumeMounts:
  #     - name: ipa-ca
  #       mountPath: /etc/ipa/ca.crt