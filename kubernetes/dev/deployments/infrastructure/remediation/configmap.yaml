apiVersion: v1
kind: ConfigMap
metadata:
  name: remediation-script
  namespace: kube-system
data:
  remediation.py: |
    # Remediation script — placeholder
    print("Remediation pod started successfully")
    
    import time
    while True:
        print("Running — script not yet implemented")
        time.sleep(120)